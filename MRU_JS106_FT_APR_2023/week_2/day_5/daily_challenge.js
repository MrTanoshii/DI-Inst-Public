let userInput = prompt();

let currentIndex = 0;
while (userInput > 0) {
  currentIndex++;
  // console logging Take somesomething...
  let remainingBottles = userInput - currentIndex;
  // console logging we have ...
  // if currentIndex == 1, write "it"
  // else, write "them"
  // if currentIndex > 1, write "then, "
}
